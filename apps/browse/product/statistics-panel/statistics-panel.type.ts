import { Reactivity } from '@browse/product/product.type'

type CountWrapProps = {
  label: string
  count: number
}

type StatisticsPanelPropTypes = {
  reactivity: Reactivity
}

export type { CountWrapProps, StatisticsPanelPropTypes }
