const variants = ['primary', 'secondary', 'tertiary', 'quaternary', 'quinary']
const disabled = [true, false]
const sizes = ['small', 'medium', 'large']

export const STORYBOOK_CONSTANTS = {
  variants,
  disabled,
  sizes,
}
